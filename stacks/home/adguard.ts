export function configureAdGuard(_args: { clients: { name: string; ip: string }[] }) {
  //   for (const client of args.clients) {
  //     new Client(`adguard-${client.name}`, {
  //         name: client.name,
  //         ids: [client.ip],
  //         ignoreQuerylog: true,
  //         ignoreStatistics: true,
  //     });
  //   }
}
