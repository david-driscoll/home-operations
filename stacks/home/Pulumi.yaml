name: home
description: Configure base homelab services, and services.
runtime:
  name: nodejs
  options:
    packagemanager: npm
    typescript: true
config:
  pulumi:tags:
    value:
      pulumi:template: typescript