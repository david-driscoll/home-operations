scrape_configs:
  # AdGuard Exporter - DNS metrics (alpha-site only)
  - job_name: 'adguard'
    static_configs:
      - targets: ['adguard-exporter:9617']
        labels:
          host: 'alpha-site'
          service: 'dns'
