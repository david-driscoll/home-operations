---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./helmrelease.yaml
  - ./definition.yaml
  # These three together are what makes ExtAuth safe. Do not drop one without
  # reading the header of each.
  - ./middleware.yaml
  - ./tailnet-subsonic.yaml
  - ./ciliumnetworkpolicy.yaml
