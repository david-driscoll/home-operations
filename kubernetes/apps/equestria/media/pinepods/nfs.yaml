---
# yaml-language-server: $schema=https://raw.githubusercontent.com/yannh/kubernetes-json-schema/refs/heads/master/master-standalone-strict/persistentvolume-v1.json
apiVersion: v1
kind: PersistentVolume
metadata:
  name: spike-media-downloads-${APP}
  labels:
    kustomize.toolkit.fluxcd.io/force: enabled
spec:
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  mountOptions:
    - nfsvers=4.2
    - hard
    - intr
    - nconnect=8
    - noatime
  capacity:
    storage: 2Ti
  storageClassName: nfs-csi
  csi:
    driver: nfs.csi.k8s.io
    volumeHandle: spike-media-downloads-${APP}-volume-id
    volumeAttributes:
      server: "${SPIKE_IP}"
      share: /mnt/stash/media/downloads/podcasts/
---
# yaml-language-server: $schema=https://raw.githubusercontent.com/yannh/kubernetes-json-schema/refs/heads/master/master-standalone-strict/persistentvolumeclaim-v1.json
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: spike-media-downloads-${APP}
  labels:
    kustomize.toolkit.fluxcd.io/force: enabled
spec:
  storageClassName: nfs-csi
  volumeName: spike-media-downloads-${APP}
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Ti
