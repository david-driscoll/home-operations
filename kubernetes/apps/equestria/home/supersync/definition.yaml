---
# yaml-language-server: $schema=https://raw.githubusercontent.com/david-driscoll/home-operations/refs/heads/main/schemas/definition.schema.json
apiVersion: driscoll.dev/v1
kind: ApplicationDefinition
metadata:
  name: ${APP}
spec:
  name: "SuperSync"
  category: Applications
  description: "Sync server for Super Productivity -- one account per authentik user"
  url: &url https://${APP}.${ROOT_DOMAIN}
  icon: https://cdn.jsdelivr.net/gh/homarr-labs/dashboard-icons/png/super-productivity.png
  # Who may use SuperSync, checked twice. The authentik binding decides who can
  # open the coordinator page and get a token. SuperSync's own ALLOWED_EMAILS
  # never runs, because its registration routes are not routed.
  #
  # serviceAccount makes the applications stack create a view_user-only
  # authentik account and publish it, together with this group list, to
  # OpenBao. The coordinator's /authz uses it to re-check on every sync
  # request, so removing someone from `family` stops their devices within
  # minutes instead of when their token expires a year later.
  access_policy:
    groups:
      - family
    serviceAccount: true
  # forward_single guards only the coordinator page (`/`). The sync API
  # (/api/sync/*) is routed around it in ./helmrelease.yaml: the desktop and
  # mobile apps send a bearer token and cannot follow an authentik redirect.
  authentik:
    proxy:
      mode: "forward_single"
      externalHost: *url
  gatus:
    - group: "Applications"
      # /health is on the unauthenticated sync side, so this checks SuperSync
      # itself (including its database) rather than the outpost's login page.
      url: https://${APP}.${ROOT_DOMAIN}/health
      method: GET
      conditions:
        - "[STATUS] == 200"
