---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: ${APP}-env
  namespace: equestria
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: openbao
  refreshPolicy: Periodic
  refreshInterval: 4m
  target:
    name: ${APP}-env
    creationPolicy: Owner
    deletionPolicy: Retain
    template:
      data:
        TZ: "${TIMEZONE}"
        MEILI_NO_ANALYTICS: "true"
        MEILI_MASTER_KEY: "{{ .meili_password }}"
  dataFrom:
    - extract:
        # was: MeiliSearch Secret Key
        key: clusters/equestria/apps/meilisearch/secret-key
      rewrite:
        - regexp:
            source: "[\\W]"
            target: _
        - regexp:
            source: "(.*)"
            target: "meili_$1"
