---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./helmrelease.yaml
  - ./definition.yaml
  - ./externalsecret.yaml
  - ./middleware.yaml
  - ./ingressroute-mcp.yaml
  - ./mcp-tailnet-ks.yaml
# ./mcp-tailnet/ is deliberately absent: it is rendered into the `network`
# namespace by ./mcp-tailnet-ks.yaml, not into this build.
