# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app postgres-backup
spec:
  chartRef:
    kind: OCIRepository
    name: app-template
  maxHistory: 3
  interval: 1h
  timeout: 10m
  install:
    createNamespace: true
    replace: true
    remediation:
      retries: 7
  upgrade:
    crds: CreateReplace
    cleanupOnFail: true
    remediation:
      retries: 7
      strategy: rollback
  rollback:
    force: false
    cleanupOnFail: true
    recreate: true
  uninstall:
    keepHistory: false
  dependsOn:
    - name: postgres
      namespace: database
    - name: external-secrets
      namespace: kube-system
  values:
    # Reads the per-database credential Secrets in this namespace directly.
    # `get` only, and only in this namespace: the CronJob never lists Secrets,
    # so a Role beats a ClusterRole and a name-scoped read beats a wildcard.
    serviceAccount:
      *app : {}

    rbac:
      roles:
        *app :
          type: Role
          rules:
            - apiGroups: [""]
              resources: ["secrets"]
              verbs: ["get"]
      bindings:
        *app :
          type: RoleBinding
          roleRef:
            identifier: *app
          subjects:
            - identifier: *app

    controllers:
      *app :
        annotations:
          reloader.stakater.com/auto: "true"
        cronjob:
          backoffLimit: 6
          concurrencyPolicy: Forbid
          failedJobsHistory: 3
          schedule: "0 2 * * *"  # Daily at 2 AM
          startingDeadlineSeconds: 30
          successfulJobsHistory: 3
          suspend: false
        type: cronjob
        pod:
          automountServiceAccountToken: true
        serviceAccount:
          identifier: *app
        containers:
          main:
            image:
              repository: mcr.microsoft.com/dotnet/sdk
              tag: '10.0-noble@sha256:e1ffd2a92ae84c1291bc1b6887501f8af98e6331e7af6d4c8d37168c5e87a64c'
              pullPolicy: IfNotPresent
            env:
              # Explicit in the manifest rather than inferred from the
              # service-account token dir, so the namespace the credentials are
              # read from is visible where the RBAC that permits it is.
              POD_NAMESPACE:
                valueFrom:
                  fieldRef:
                    fieldPath: metadata.namespace
              # Databases still in postgres whose application has been removed.
              # They have no credential Secret, so they are named here rather
              # than being skipped by a "no Secret? never mind" rule -- a
              # missing Secret on a LIVE database must stay a hard failure.
              # Their existing dumps are left untouched; nothing is deleted.
              #   keeper   -- app removed 2026-03-26 (6a2139535)
              #   vikunja  -- app removed 2026-07-27 (0b289652f)
              DECOMMISSIONED_DATABASES: "keeper,vikunja"
            command:
              - /bin/sh
              - -c
              # postgresql-client-18 is COUPLED to the CNPG cluster's major:
              # pg_dump aborts against a server newer than itself, so this pin
              # must never lag database/postgres/app/resources/values.yaml. It
              # may lead it -- an 18 client dumps a 17 server fine -- which is
              # why it moved first, in the same change that prepared the 17->18
              # upgrade. Same constraint, same reason, as the openbao-replica
              # nightly dump in kube-system/openbao-replica.
              - >-
                set -eux;
                export DEBIAN_FRONTEND=noninteractive;
                apt-get update
                && apt install -y postgresql-common
                && /usr/share/postgresql-common/pgdg/apt.postgresql.org.sh -y
                && apt-get install -y --no-install-recommends postgresql-client-18
                && dotnet run /scripts/App.cs
            resources:
              requests:
                memory: 256Mi
                cpu: 20m
              limits:
                memory: 512Mi
                cpu: 500m

    # defaultPodOptions:
    #   securityContext:
    #     runAsUser: 999
    #     runAsGroup: 999
    #     fsGroup: 999
    #     runAsNonRoot: true
    #     fsGroupChangePolicy: "OnRootMismatch"
    #     seccompProfile:
    #       type: RuntimeDefault

    persistence:
      script:
        type: configMap
        name: postgres-backup-script
        defaultMode: 493
        globalMounts:
          - path: /scripts/App.cs
            subPath: App.cs
      tmp:
        type: emptyDir
        globalMounts:
          - path: /tmp
            subPath: tmp
          - path: /.dotnet
            subPath: dotnet
      backups:
        type: nfs
        server: ${SPIKE_IP}
        path: /mnt/stash/data/pgdump
