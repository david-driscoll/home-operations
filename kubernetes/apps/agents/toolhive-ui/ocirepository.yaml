---
# yaml-language-server: $schema=https://raw.githubusercontent.com/fluxcd-community/flux2-schemas/main/ocirepository-source-v1.json
apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: toolhive-cloud-ui
spec:
  interval: 1h
  layerSelector:
    mediaType: application/vnd.cncf.helm.chart.content.v1.tar+gzip
    operation: copy
  ref:
    tag: 0.6.2
    digest: sha256:e487d7db2d1f3df9a4da484ad910e26356b326282e06ac92c68256b8e6d28dbd
  # A DIFFERENT OCI path from the operator's. The UI ships from
  # stacklok/toolhive-cloud-ui/, not stacklok/toolhive/, and it has its own
  # release train -- 0.6.2 here against the operator's 0.44.0. Renovate tracks
  # the two independently; do not expect them to move together.
  url: oci://ghcr.io/stacklok/toolhive-cloud-ui/toolhive-cloud-ui
  # No `verify:` block, for the same reason as the operator charts next door:
  # no cosign signature is published for these digests. The digest pin is the
  # protection.
