---
# yaml-language-server: $schema=https://raw.githubusercontent.com/datreeio/CRDs-catalog/main/external-secrets.io/externalsecret_v1.json
#
# The SAME Pulumi credential bundle ../agentboard/externalsecret.yaml pulls,
# from the SAME OpenBao paths the `pulumi` namespace's own workspace pods use
# (kubernetes/apps/pulumi/secrets/, kubernetes/apps/pulumi/*/stack.yaml) --
# deliberately not re-minted, so rotating one Pulumi credential rotates it
# for every consumer at once.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: ${APP}
  annotations:
    reloader.stakater.com/auto: "true"
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: openbao
  refreshPolicy: Periodic
  refreshInterval: 4m
  target:
    name: ${APP}
    creationPolicy: Owner
    template:
      metadata:
        annotations:
          reloader.stakater.com/auto: "true"
      data:
        AWS_ACCESS_KEY_ID: "{{ .minio_username }}"
        AWS_SECRET_ACCESS_KEY: "{{ .minio_password }}"
        AWS_REGION: "home"
        PULUMI_CONFIG_PASSPHRASE: "{{ .pulumi_passphrase }}"
        CONNECT_TOKEN: "{{ .connect_token }}"
        AUTHENTIK_TOKEN: "{{ .authentik_token }}"
        AUTHENTIK_URL: "{{ .authentik_url }}"
        GITHUB_TOKEN: "{{ .github_token }}"
  data:
    - secretKey: minio_username
      remoteRef:
        key: apps/minio/root
        property: username
    - secretKey: minio_password
      remoteRef:
        key: apps/minio/root
        property: password
    - secretKey: pulumi_passphrase
      remoteRef:
        key: apps/pulumi/passphrase
        property: password
    - secretKey: connect_token
      remoteRef:
        key: third-party-tokens/onepassword/eris-connect
        property: credential
    - secretKey: authentik_token
      remoteRef:
        key: clusters/alpha-site/apps/authentik/token
        property: credential
    - secretKey: authentik_url
      remoteRef:
        key: clusters/alpha-site/apps/authentik/token
        property: url
    # Own path, NOT ../agentboard's -- this pod is a separate identity, and a
    # PAT scoped for the MCP server should be revocable independently of the
    # one the agentboard pod's own git operations use.
    #
    #   bao kv put secrets/clusters/${CLUSTER_CNAME}/apps/${APP}/github \
    #     token="..."
    - secretKey: github_token
      remoteRef:
        key: clusters/${CLUSTER_CNAME}/apps/${APP}/github
        property: token
