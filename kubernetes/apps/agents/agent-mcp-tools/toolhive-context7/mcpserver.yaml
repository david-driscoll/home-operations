---
# No `yaml-language-server: $schema=` line -- see ../../toolhive-docs/mcpserver.yaml.
#
# @upstash/context7-mcp (npm) has no prebuilt container of its own, so this
# runs it on the stock `node` image the same way the unifi-* servers run
# PyPI packages on `uv` -- podTemplateSpec overrides `command`/`args` to
# `npx -y @upstash/context7-mcp` explicitly rather than relying on the
# image's own ENTRYPOINT/CMD (unverified for this specific tag; the uv-based
# servers next door needed this same override once verified, so it is
# assumed rather than re-checked per image). No digest pin yet -- add one
# before merging.
#
# No CONTEXT7_API_KEY -- works unauthenticated at Upstash's public rate
# limit for now. Add one via a new OpenBao path if that turns out to be too
# low for this namespace's usage.
#
# `transport: stdio` because the package speaks stdio -- the operator's
# proxy-runner is what turns that into Streamable HTTP, same as every
# npx/uvx-wrapped server here.
#
# NO oidcConfigRef/authServerRef -- see ../toolhive-kubernetes/mcpserver.yaml's
# header. This server joins ../../toolhive-agent-tools's MCPGroup instead.
apiVersion: toolhive.stacklok.dev/v1beta1
kind: MCPServer
metadata:
  name: ${APP}
spec:
  image: node:22-alpine
  transport: stdio
  proxyPort: 8080
  proxyMode: streamable-http
  groupRef:
    name: agent-tools

  podTemplateSpec:
    spec:
      containers:
        - name: mcp
          command: ["npx"]
          args: ["-y", "@upstash/context7-mcp"]
          # Flagged by Codacy (real finding, unlike the httproute.yaml "id
          # field" noise elsewhere in this PR -- a podTemplateSpec container
          # override like this one can silently drop whatever hardening the
          # operator's own default pod spec would otherwise apply).
          # readOnlyRootFilesystem is NOT set here -- npx needs to write its
          # own cache/extract dir and this image has no emptyDir set up for
          # that the way the uv-based servers next door do.
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]

  resources:
    requests:
      cpu: 50m
      memory: 128Mi
    limits:
      memory: 512Mi
