---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: &app grafana-secret
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: openbao
  refreshPolicy: Periodic
  refreshInterval: 4m
  target:
    name: *app
    template:
      engineVersion: v2
      data:
        GF_DATABASE_NAME: "{{ .postgres_database }}"
        GF_DATABASE_HOST: "{{ .postgres_hostname }}:{{ .postgres_port }}"
        GF_DATABASE_USER: "{{ .postgres_username }}"
        GF_DATABASE_PASSWORD: "{{ .postgres_password }}"
        GF_DATABASE_SSL_MODE: disable
        GF_DATABASE_TYPE: postgres
        GF_AUTH_OAUTH_AUTO_LOGIN: "true"
        GF_AUTH_OAUTH_LOGIN_PROMPT: "login"
        GF_AUTH_GENERIC_OAUTH_ENABLED: "true"
        GF_AUTH_SIGNOUT_REDIRECT_URL: "{{ .oidc_end_session_url }}"
        GF_AUTH_GENERIC_OAUTH_NAME: ${CLUSTER_TITLE} Grafana
        GF_AUTH_GENERIC_OAUTH_CLIENT_ID: "{{ .oidc_client_id }}"
        GF_AUTH_GENERIC_OAUTH_CLIENT_SECRET: "{{ .oidc_client_secret }}"
        GF_AUTH_GENERIC_OAUTH_AUTH_URL: "{{ .oidc_authorization_url }}"
        GF_AUTH_GENERIC_OAUTH_API_URL: "{{ .oidc_userinfo_url }}"
        GF_AUTH_GENERIC_OAUTH_TOKEN_URL: "{{ .oidc_token_url }}"
        GF_AUTH_GENERIC_OAUTH_NAME_ATTRIBUTE_PATH: "nickname"
        GF_AUTH_GENERIC_OAUTH_SCOPES: openid profile email grafana_role
        GF_AUTH_GENERIC_OAUTH_GROUPS_ATTRIBUTE_PATH: "groups"
        GF_AUTH_GENERIC_OAUTH_EMAIL_ATTRIBUTE_NAME: "email"
        GF_AUTH_GENERIC_OAUTH_ROLE_ATTRIBUTE_PATH: "contains(groups[*], 'admins') && 'GrafanaAdmin' || contains(groups[*], 'media-managers') && 'Editor' || 'Viewer'"
  dataFrom:
    - extract:
        # was: '${CLUSTER_CNAME}-${APP}-oidc-credentials'
        # The substitution variables carry straight over — Phase 8a writes this
        # family to clusters/<cluster>/apps/<app>/oidc, so the path is derivable
        # from the same two vars rather than needing to be spelled out.
        key: 'clusters/${CLUSTER_CNAME}/apps/${APP}/oidc'
      rewrite:
        - regexp:
            source: "[\\W]"
            target: _
        - regexp:
            source: "(.*)"
            target: "oidc_$1"
    - extract:
        key: '${APP}-postgres'
      sourceRef:
        storeRef:
          kind: ClusterSecretStore
          name: database
      rewrite:
        - regexp:
            source: "[\\W]"
            target: _
        - regexp:
            source: "(.*)"
            target: "postgres_$1"
---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: &app grafana-admin-secret
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: openbao
  refreshPolicy: Periodic
  refreshInterval: 4m
  target:
    name: *app
    template:
      engineVersion: v2
      data:
        # Admin User
        admin-user: "{{ .grafana_username }}"
        admin-password: "{{ .grafana_password }}"
  dataFrom:
    - extract:
        # was: Grafana Credentials
        key: shared/grafana-credentials
      rewrite:
        - regexp:
            source: "[\\W]"
            target: _
        - regexp:
            source: "(.*)"
            target: "grafana_$1"
