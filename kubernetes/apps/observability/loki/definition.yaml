---
# yaml-language-server: $schema=https://raw.githubusercontent.com/david-driscoll/stargate-command-cluster/refs/heads/main/schemas/definition.schema.json
apiVersion: driscoll.dev/v1
kind: ApplicationDefinition
metadata:
  name: loki
spec:
  name: Loki
  category: Infrastructure
  description: Loki is a log aggregation system designed to work with Prometheus.
  url: &url https://loki.${ROOT_DOMAIN}
  icon: https://cdn.jsdelivr.net/gh/homarr-labs/dashboard-icons/svg/loki.svg
  access_policy:
    groups:
      - admins
  authentik:
    proxy:
      mode: "forward_single"
      externalHost: *url
  gatus:
    - url: https://loki.${ROOT_DOMAIN}/ready
      method: GET
      conditions:
        - "[STATUS] == 200"
