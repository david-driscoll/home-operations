---
apiVersion: tailscale.com/v1alpha1
kind: DNSConfig
metadata:
  name: tailscale-nameserver
spec:
  nameserver:
    service:
      clusterIP: ${TAILSCALE_NAMESERVER_IP}
    image:
      repo: tailscale/k8s-nameserver
      tag: unstable
