server:
  proxied: true

auth:
  secret-key: ${DYNACAT_SECRET_KEY}
  require-auth: false
  disable-password: true
  oidc:
    issuer-url: ${DYNACAT_OIDC_ISSUER_URL}
    client-id: ${DYNACAT_OIDC_CLIENT_ID}
    client-secret: ${DYNACAT_OIDC_CLIENT_SECRET}
    redirect-url: ${DYNACAT_OIDC_REDIRECT_URL}
    scopes:
      - openid
      - profile
      - email
    groups-claim: groups

document:
  head: |
    <style>
      /* Hide uptime text in head-widget server cards */
      .head-widgets .server .server-details :nth-child(2) {
        display: none;
      }

      /* Side-by-side layout for head-widgets */
      .head-widgets {
        display: flex;
        flex-direction: row;
        gap: var(--widget-gap);
        align-items: stretch;
      }
      .head-widgets .widget {
        flex: 1;
        min-width: 0;
        margin-top: 0 !important;
      }

      /* Condense head-widget server cards */
      .head-widgets .widget-content:not(.widget-content-frameless) {
        padding: 8px 12px;
      }
      .head-widgets .server {
        gap: 0.4rem;
      }
      .head-widgets .server + .server {
        margin-top: 0.5rem;
      }
      .head-widgets .server-name {
        font-size: var(--font-size-h4);
      }
      .head-widgets .server-icon {
        width: 2rem;
        height: 2rem;
      }
      .head-widgets .server-stats {
        gap: 0.8rem;
        margin-top: 0;
        padding-bottom: 0;
      }
      .head-widgets .progress-bar {
        height: 1rem;
      }
      @container widget (min-width: 650px) {
        .head-widgets .server-stats {
          min-width: 280px;
          padding-bottom: 0;
          gap: 1rem;
        }
        .head-widgets .server-stats > * {
          max-width: 150px;
        }
        .head-widgets .server + .server {
          margin-top: 0.4rem;
        }
      }
    </style>

# branding:
#   custom-footer: |
#     <p>Powered by <a href="https://github.com/Panonim/dynacat">Dynacat</a></p>
#   logo-url: /assets/logo.png
#   favicon-url: /assets/logo.png
#   app-name: "My Dashboard"
#   app-icon-url: "/assets/app-icon.png"
#   app-background-color: "#151519"
# theme:
#   # This will be the default theme
#   background-color: 240 21 15
#   contrast-multiplier: 1.2
#   primary-color: 129 17 223
#   positive-color: 115 54 76
#   negative-color: 347 70 65
#   disable-picker: false
#   presets:
#     teal:
#       light: false
#       background-color: 225 14 15
#       primary-color: 157 47 65
#       contrast-multiplier: 1.1
#     purple:
#       light: false
#       background-color: 50 1 6
#       primary-color: 24 97 58
#       negative-color: 209 88 54

pages:
  - $include: home.yaml
  - $include: homelab.yaml
  - $include: high-seas.yaml
  - $include: servers/alpha-site.yaml
  - $include: servers/sgc.yaml
  - $include: servers/equestria.yaml
  - $include: servers/celestia.yaml
  - $include: servers/luna.yaml
  - $include: servers/skystar.yaml
  - $include: staging.yaml
  # - $include: videos.yaml
